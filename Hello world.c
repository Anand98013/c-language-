#include<stdio.h>
#include<conio.h>
 
void main()
{
   textcolor(RED); // You could type "4" instead of "RED", but it is not as readable
   cprintf("Hello, World!");
 
   getch();
   return 0;
}